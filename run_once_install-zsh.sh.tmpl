#!/bin/sh

{{ if env "CODESPACES" -}}
sudo sh -c "echo \"vscode ALL=(ALL) NOPASSWD:ALL\" >> /etc/sudoers"
{{ end -}}

{{ if eq .chezmoi.os "linux" -}}
sudo apt install -y zsh
{{ else if eq .chezmoi.os "darwin" -}}
brew install zsh
{{ end -}}

chsh -s $(which zsh)
